#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
typedef long long ll;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int a, b;
    string x;

    cin >> a >> b >> x;

    cout << a << " + " << b << " = " << a + b << "\n" << x << endl;
}